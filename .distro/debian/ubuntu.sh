
sudo apt-get install ubuntu-desktop-minimal

# Ubuntu Core 
sudo apt  install gnome-core

# MIN
1. Install Ubuntu Server 22.04 (minimal)
2. Install openbox, xorg
3. Disable unneccessary services (sudo systemctl disable ...)
4. Only install the GUI apps you actually need
5. Linux desktop using 200-300MB RAM and 2GB disk storage